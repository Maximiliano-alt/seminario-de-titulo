version: "3.8"
services:
  backend:
    build:
      context: ./Backend
      dockerfile: dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env

  frontend:
    build:
      context: ./Frontend
      dockerfile: dockerfile
    ports:
      - "5173:80"
    depends_on:
      - backend
